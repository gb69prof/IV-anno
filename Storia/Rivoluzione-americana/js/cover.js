(() => {'use strict';let installEvent;const status=document.getElementById('offline-status');window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();installEvent=e;document.querySelector('[data-action="install"]').hidden=false});document.querySelector('[data-action="install"]').addEventListener('click',async()=>{if(installEvent){await installEvent.prompt();installEvent=null}});document.querySelector('.skip-link').addEventListener('click',e=>{e.preventDefault();document.getElementById('cover').focus()});if('serviceWorker'in navigator){navigator.serviceWorker.register('service-worker.js').then(()=>navigator.serviceWorker.ready).then(()=>{const update=()=>status.textContent=navigator.onLine?'Percorso pronto offline':'Sei offline · Percorso disponibile';update();window.addEventListener('online',update);window.addEventListener('offline',update)}).catch(()=>{status.textContent='Offline non pronto: riapri il percorso con una connessione sicura.'})}else status.textContent='Offline non disponibile in questo browser.'})();
